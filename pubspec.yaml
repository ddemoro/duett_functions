name: duett
description: A dating app that elimates those akward solo dates
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.8+1

environment:
  sdk: '>=2.19.0 <3.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  firebase_core: ^3.5.0
  firebase_storage: ^12.3.1
  firebase_messaging: ^15.2.4
  cloud_firestore: ^5.4.2
  firebase_auth: ^5.3.0
  url_launcher: ^6.1.10
  intl_phone_number_input: ^0.7.3+1
  google_sign_in: ^6.2.1
  sign_in_with_apple: ^6.1.2
  unicons: any
  eva_icons_flutter: ^3.1.0
  flutter_cupertino_date_picker_fork: ^1.0.7
  notification_permissions: ^0.6.1
  string_validator: ^1.0.0
  geolocator: ^13.0.1
  geocoding: ^3.0.0
  permission_handler: ^11.3.1
  image_picker: ^1.1.2
  image_cropper: ^8.0.2
  uuid: ^4.5.0
  dotted_border: ^2.0.0+3
  carousel_slider: ^5.0.0
  contacts_service: ^0.6.3
  simple_moment: ^1.1.1
  flutter_spinkit: ^5.2.0
  photo_view: ^0.15.0
  flutter_sms: ^2.3.3
  badges: ^3.1.2
  reorderable_grid_view: ^2.2.6
  google_places_flutter: ^2.0.6
  camera: ^0.11.0+2
  video_player: ^2.9.1
  ffmpeg_kit_flutter_full_gpl: ^6.0.3
  path_provider: ^2.1.1
  cached_network_image: ^3.4.1

  intl: any
  connectivity_plus: ^6.1.4
  flutter_cache_manager: ^3.4.1
dev_dependencies:
  flutter_launcher_icons: ^0.14.0
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^4.0.0


flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/app_icon.png"
  min_sdk_android: 21 # android min sdk min:16, default 21
  web:
    generate: true
    image_path: "assets/images/app_icon.png"
    background_color: "#hexcode"
    theme_color: "#hexcode"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  fonts:
    - family: Recoleta
      fonts:
        - asset: assets/fonts/recoleta_alt_bold.otf
    - family: Caros
      fonts:
        - asset: assets/fonts/caros/caros-regular.otf
        - asset: assets/fonts/caros/caros-medium.otf
        - asset: assets/fonts/caros/caros-bold.otf
    - family: Kanit
      fonts:
          - asset: assets/fonts/kanit/Kanit-Regular.ttf
          - asset: assets/fonts/kanit/Kanit-Medium.ttf
          - asset: assets/fonts/kanit/Kanit-Bold.ttf

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/background.png
    - assets/images/gradient.png
    - assets/images/better_with_friends.png
    - assets/images/logo.png
    - assets/images/white_logo.png
    - assets/images/cake.png
    - assets/images/confirm_overlay.png
    - assets/images/how_duett_works.png
    - assets/images/how_duett_works2.png
    - assets/images/how_duett_works3.png
    - assets/images/how_duett_works4.png
    - assets/images/default_feed_image.png
    - assets/images/dots.png
    - assets/images/filter_alt.png
    - assets/images/microphone.png
    - assets/images/pin_alt.png
    - assets/images/play.png
    - assets/images/sound_mute.png
    - assets/images/listen.png
    - assets/images/info.png
    - assets/images/education.png
    - assets/images/job.png
    - assets/images/living.png
    - assets/images/political_view.png
    - assets/images/religion.png
    - assets/images/picture_2.png
    - assets/images/running.png
    - assets/images/stretching.png
    - assets/images/pass.png
    - assets/images/like.png
    - assets/images/duett_nav_logo.png
    - assets/images/demo/person1.png
    - assets/images/demo/person2.png
    - assets/images/demo/person3.png
    - assets/images/demo/person4.png
    - assets/images/demo/person5.png
    - assets/images/demo/person6.png
    - assets/images/demo/person7.png
    - assets/images/demo/person8.png
    - assets/images/demo/time_light.png
    - assets/images/demo/match.png
    - assets/images/demo/andrea.png
    - assets/images/demo/mick.png
    - assets/images/waiting_list.png
    - assets/images/friends_splash.png
    - assets/images/splash.png
    - assets/images/profile.png
    - assets/images/inactive_icon.png
    - assets/images/active_icon.png



  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
